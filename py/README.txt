Just open index.html.

Tested the code snippets in a VirtualEnv Python Environment console.  Check requirements.txt for the Python packages used.

I setup my Python like this:

$ python virtualenv.py .
$ pip install -r requirements.txt

